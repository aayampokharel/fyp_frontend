import 'package:flutter/material.dart';

class ColorConstants {
  // Primary Colors (from logo)
  static const Color yellowGold = Color(
    0xFFF5A623,
  ); // Background circle, highlights, primary buttons
  static const Color darkBrown = Color(
    0xFF1C1C1C,
  ); // Main text, headings, icons, outlines
  static const Color lightBrown = Color(
    0xFFD9986E,
  ); // Secondary buttons, accent elements, hover effects

  // Complementary / Neutral Colors
  static const Color white = Color(
    0xFFFFFFFF,
  ); // Text on dark backgrounds, cards, tooltips
  static const Color lightGray = Color(
    0xFFF0F0F0,
  ); // Backgrounds, borders, input fields
  static const Color darkGray = Color(
    0xFF4B4B4B,
  ); // Secondary text, disabled buttons, subtle dividers

  // Hover / Active States
  static const Color yellowGoldDark = Color(
    0xFFE5941F,
  ); // Darker shade of yellow for hover/active buttons
  static const Color darkBrownDark = Color(
    0xFF1A1A1A,
  ); // Darker shade of brown for hover/active states

  static const Color primaryPurple = Color(0xFF6A1B9A);
  static const Color accentPurple = Color(0xFF8E24AA);
  static const Color softPurple = Color(0xFFEDE7F6);
  static const Color extraLightGray = Color.fromRGBO(
    246,
    247,
    251,
    1,
  ); // soft background
}
